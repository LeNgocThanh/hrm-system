export enum OvertimeStatus {
  Pending = 'pending',
  Approved = 'approved',
  Rejected = 'rejected',
  Cancelled = 'cancelled',
}
export enum OvertimeKind {
  Weekday = 'WEEKDAY',
  Weekend = 'WEEKEND',
  Holiday = 'HOLIDAY',
}

export enum CompensationType {
  Pay = 'PAY',
  TimeOff = 'TIME_OFF',
}
